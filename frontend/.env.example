# Local dev needs no env vars here — next.config.js proxies /api/* to
# http://127.0.0.1:8000 by default. To point the built app at a different
# backend (e.g. in docker-compose), set API_PROXY_HOST as a build-time arg
# (see frontend/Dockerfile / docker-compose.yml), not as a runtime env var.
